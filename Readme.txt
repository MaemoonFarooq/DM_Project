README
1.Libraries

Make sure to download all dependencies such as tensorflow and pmdarima the following lines will download these dependencies
pip install pmdarima
pip install tensorflow

open in colab then only download pmdarima

2. File configuration

if using colab simply upload the provided datasets to colab and run the code else give path the start to the specific dataset

3. Run

just run all cells and get the output
